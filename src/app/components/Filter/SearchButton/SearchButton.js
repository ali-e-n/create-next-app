import React from "react";

const SearchButton = () => {
  return (
    <div className="search ">
      <input
        type="Search"
        aria-label="Search"
        placeholder="Search by kyeword"
        className="btnStructure"
      />
    </div>
  );
};

export default SearchButton;
